<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aplikasikaryaanakbangkit.sentiment">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SentimentAnalysisOfVaccinationApp"
        tools:ignore="AllowBackup">
        <activity
            android:name=".splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.SentimentAnalysisOfVaccinationApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity" />
        <activity
            android:name=".news.detail.DetailNewsActivity"
            android:theme="@style/DetailTheme" />
        <activity
            android:name=".news.detail.DetailVaccineNewsActivity"
            android:theme="@style/DetailTheme"/>
    </application>

</manifest>